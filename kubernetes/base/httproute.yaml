apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: odoo-httproute
spec:
  parentRefs:
  - name: odoo-gateway
  hostnames:
  - "walidb.fr"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: odoo-service
      port: 8069
